<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seasonal.mapper.MerchantAddressMapper">
    <resultMap id="merchantAddress" type="com.seasonal.pojo.MerchantAddress">

    </resultMap>

    <select id="findAllMerchantAddress" resultMap="merchantAddress">
        select * from merchant_address
    </select>

</mapper>