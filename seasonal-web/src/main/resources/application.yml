server:
  port: 443
  ssl:
    key-store: www.strin.xyz.jks
    key-store-password: 3jeuv93sz1
    key-store-type: JKS
    key-alias: www.strin.xyz

spring:
  thymeleaf:
    cache: false
    enabled: false
    http:
      multipart:
        enabled: true
        max-file-size: 1MB
        max-request-size: 3MB
  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .jsp
  #加载banner
  output:
    ansi:
      enabled: always
  #配置MySQL
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    url: jdbc:mysql://127.0.0.1:3306/seasonal?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
      #49.235.249.18
    username: root
    password: 123456
    #username: root
     # password: Guo_19980502
    driverClassName: com.mysql.cj.jdbc.Driver
  data:
    #配置mongodb
    mongodb:
      database: seasonal
      host: 127.0.0.1
      port: 27017
      username:
      password:
      uri: mongodb://127.0.0.1
    #配置elasticsearch
    elasticsearch:
      cluster-name: my-application
      cluster-nodes: 127.0.0.1:9300
  #配置Redis
  redis:
    database: 0
    timeout: 10000
    # Redis服务器地址
    host: 127.0.0.1
    # Redis服务器连接端口
    port: 6379
    # Redis服务器连接密码（默认为空）
    password:
    # 连接池最大连接数（使用负值表示没有限制）


  #39.105.184.0
  application:
    name: rabbitmq-demo
  rabbitmq:
    host: localhost
    password: guest
    port: 15672
    username: guest

#logging:
#  file: E:/boot.log
#  level:
#    com:
#      seasonal: debug

logging:
  file: E:/boot.log
  level:
    com:
      seasonal: debug
mybatis:
  type-aliases-package: com.seasonal.pojo
  configuration:
    map-underscore-to-camel-case: true
  #路径,在webapp文件夹下新建文件夹WEB-INF,在往下建文件夹jsp




