server:
  port: 443
  ssl:
    key-store: www.strin.xyz.jks
    key-store-password: 3jeuv93sz1
    key-store-type: JKS
    key-alias: www.strin.xyz

spring:
  thymeleaf:
    cache: false
    enabled: false
  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .jsp
  #加载banner
  output:
    ansi:
      enabled: always
  #    #热部署启动
  #  thymeleaf:
  #    cache: true
  #    devtools:
  #      restart:
  #        enabled: true
  #        additional-paths: src/main/java
  #配置MySQL
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    url: jdbc:mysql://49.235.249.18:3306/seasonal?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: Guo_19980502
    driverClassName: com.mysql.cj.jdbc.Driver
  #配置Redis
  redis:
    database: 0
    timeout:
    # Redis服务器地址
    host: 127.0.0.1
    # Redis服务器连接端口
    port: 6379
    # Redis服务器连接密码（默认为空）
    password:
    # 连接池最大连接数（使用负值表示没有限制）

  #配置mongodb
  data:
    mongodb:
      database: seasonal
      host: 39.105.184.0
      port: 27017
      username:
      password:
      uri: mongodb://39.105.184.0



#logging:
#  file: E:/boot.log
#  level:
#    com:
#      seasonal: debug

logging:
  file: E:/boot.log
  level:
    com:
      seasonal: debug
mybatis:
  type-aliases-package: com.seasonal.pojo
  configuration:
    map-underscore-to-camel-case: true
  #路径,在webapp文件夹下新建文件夹WEB-INF,在往下建文件夹jsp




